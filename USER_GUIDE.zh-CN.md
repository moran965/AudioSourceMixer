# Audio Source Mixer 使用指南

[English](USER_GUIDE.md)

## 基本使用

安装并启动 Audio Source Mixer 后，可在每个来源卡片中调节音量、静音、左右平衡、输出设备，以及支持时的 EQ。Windows 来源使用系统原生 0–100%；浏览器增强标签页支持最高 200% 增益，超过 100% 可能失真。

峰值条仅用于显示实时电平。关闭主窗口时可让程序留在托盘；需要退出时使用托盘菜单“退出并恢复音频设置”，让程序先恢复所控制的音频状态。

从卡片右上角六点把手拖动可手动排序；卡片菜单可移动或隐藏来源。“已隐藏 N”只恢复手动隐藏来源，与浏览器聚合会话自动过滤设置相互独立。

## 输出路由

Windows 会话会显示真实路由状态：已应用、失败、断开、部分迁移或等待音频流重启。“等待重启”表示偏好已经保存，但对应应用需要重新创建音频流。

增强标签页选择新的物理输出时可能打开授权页。请选择与 Windows 名称对应的设备，播放短测试音并确认。设备改名、重连或浏览器标识变化后可能需要重新授权；程序不会把静默回退到原设备当作成功。

## 浏览器增强

1. 让已安装的桌面程序保持运行或留在托盘。
2. 在程序中打开“浏览器增强”，进入对应浏览器的扩展管理页。
3. 启用开发者模式，选择“加载已解压的扩展程序”，并选择安装目录中的 `BrowserExtension`。
4. 在标签页开始播放后点击扩展图标；再次点击可停止增强。

第一次操作可能打开本地欢迎页。阅读后返回播放标签页并再次点击。扩展只会在这次用户操作后处理标签页。

## 语言与无障碍

打开“设置 → 语言”，选择“简体中文”或“English”。当前窗口、已实例化模板、状态提示和托盘菜单会立即刷新，不会重建音频服务、重置来源或重新路由。语言设置使用独立 schema 持久保存。

程序支持键盘焦点、屏幕阅读器名称、高对比颜色和 Windows DPI 缩放。880×600 最小窗口下，使用主页面滚动条访问超出区域的内容。

## 数据与卸载

桌面数据位于 `%LocalAppData%\AudioSourceMixer`；扩展映射留在当前浏览器 profile。卸载默认保留桌面数据；仅在确实需要时选择“删除用户设置和日志”。扩展输出映射可在授权页清除，或随浏览器 profile 中的扩展数据一并删除。
