# Audio Source Mixer 使用说明

运行 `AudioSourceMixer.exe` 后，可分别调节应用或已启用浏览器标签页的音量、静音、声道平衡和输出设备。关闭窗口时程序可继续留在托盘；使用托盘菜单“退出并恢复音频设置”可安全退出。

浏览器增强需要在 Chrome/Edge 开发者模式中加载 `BrowserExtension`，并先运行 `scripts/register-native-host.ps1`。点击扩展按钮后才会捕获当前标签页；输出设备首次选择必须在可见授权页中试听并确认。软件不录音、不上传音频或网页内容。

普通 Windows 应用使用系统原生 0–100% 控制。最高 200% 增益及均衡器仅适用于用户主动启用的浏览器增强标签页。
