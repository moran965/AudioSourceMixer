# v0.1.2 开发基线（v0.1.1）

验证日期：2026-08-09（Asia/Shanghai）  
操作系统：Microsoft Windows NT 10.0.26200.0，x64  
仓库：`C:\Users\asus\Documents\audio-control`

## 源码状态

- 当前分支：`master`。
- Git commit：无。仓库已初始化，但 `master` 尚无提交；所有项目内容均为未跟踪文件。
- Git tag：无。
- `git diff`：空（未跟踪文件不出现在普通 diff 中）。
- 保护措施：全部现有未跟踪文件均按用户内容处理；未执行 reset、checkout 或覆盖式清理。
- 产品版本：0.1.1（`Directory.Build.props`）。
- 安装器版本：0.1.1；现有正式产物为 `AudioSourceMixer-0.1.1-win-x64-setup.exe`。
- Chrome/Edge 扩展版本：0.1.1，Manifest V3，最低 Chrome 116。
- Native Messaging 协议版本：1；v0.1.2 开发继续保持协议 1，除非消息结构产生不兼容变化。

由于仓库没有任何 commit 或 tag，无法执行有意义的 `git log`、`git blame` 或 v0.1.0..v0.1.1 历史差异。遗留清理改用文件版本、哈希、当前引用、打包脚本行为以及构建/测试验证进行保守判定。

## 未修改基线验证

执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\test.ps1 -Configuration Release`：

- Restore：通过。
- Release Build：通过，0 warning，0 error。
- .NET：36/36 通过（Core 33、Desktop/WPF 1、WindowsAudio 1、NativeHost 1）。
- Browser/Node：7/7 通过。
- Source Release UI smoke：通过；真实调用 `Show()` 并覆盖 ItemsControl/DataTemplate。

普通启动验证：

- Release 主程序显示窗口并完成首次布局。
- 启动日志：`WindowShown=True; Sources=5; MaterializedItems=5`。
- 正常关闭退出码：0。
- 退出后 `rollback.json` 为 `[]`，没有遗留音频回滚项。

Windows Audio 能力探测（`AudioSourceMixer.CapabilityProbe.exe --exercise`）：

- 默认 Multimedia render endpoint：`扬声器 (Realtek(R) Audio)`。
- 发现 5 个会话。
- Master Volume、Mute、双声道 round-trip 与 Peak Meter：全部可用。
- 对一个非活动测试会话执行 1.00 → 0.73、mute、左声道并恢复；`restoreMatched=true`。
- Session notification 与 device notification 均成功注册。

这份记录只证明 v0.1.1 基线健康，不代表尚未实现的 v0.1.2 增益或自定义设备路由已通过。
